{% extends template ~ "/layout/layout.twig" %} 
	{% block content %} 
<section class="mt-0" style="padding-top: 8.5rem">
	<!-- Page Content Goes Here -->
	<section class="mt-5">
		<div class="container">
			<div class="row">
				<div class="col-12">
					{% if ok %}
					<h3>{{lang.confirm_sub}}</h3>
					<p>{{lang.confirm_tuts}}</p>
					{% else %}
					<h3>{{lang.bad_sub}}</h3> 
					<p>{{lang.bad_sub_tuts}}</p>
					{% endif %}
				</div>
			</div>
	</section>
</section>
<!-- /Page Content -->
	{% endblock %}